/* var cX = 29;
var cY = 1001;

let dude = document.querySelector('#character'); */

/* window.addEventListener(
  'keydown',
  (e) => {
    while ('ArrowUp' === e.key) {
      character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
    }
     if ('ArrowUp' === e.key) {
      cY = cY - 5;
      character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
    } 
    if ('ArrowDown' === e.key) {
      cY = cY + 5;
      character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
    }
    if ('ArrowLeft' === e.key) {
      cX = cX - 5;
      character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
    }
    if ('ArrowRight' === e.key) {
      cX = cX + 5;
      character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
    }
  },
  false
); */

///////////////

/* window.addEventListener(
  'keydown',
  (e) => {
    switch (e.key) {
      case 'ArrowUp': // if (x === 'value1')
        cY = cY - 5;
        character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');
        break;

      case 'ArrowDown': // if (x === 'value2')
        cY = cY + 5;
        character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');

      case 'ArrowLeft': // if (x === 'value1')
        cX = cX - 5;
        character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');

      case 'ArrowRight': // if (x === 'value2')
        cX = cX + 5;
        character.setAttribute('transform', 'translate(' + cX + ' ' + cY + ')');

    }
  },
  false
);
 */
